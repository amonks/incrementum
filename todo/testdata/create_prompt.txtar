# setup repo
mkdir repo
cd repo
exec jj git init

# decline creation
stdin deny.txt
! exec $INCR todo list
stdout 'No todo store found\. Create one\?'

# accept creation
stdin accept.txt
exec $INCR todo list
stdout 'No todos found\.'

-- repo/deny.txt --
n
-- repo/accept.txt --
y

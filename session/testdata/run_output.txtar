# setup repo
mkdir repo
cd repo
exec jj git init

# create todo
stdin input.txt
exec $INCR todo create 'Run todo'

exec $INCR todo list --json
todoid stdout 'Run todo' TODO_ID

exec $INCR session run $TODO_ID -- true
stdout 'marked completed'

-- repo/input.txt --
y

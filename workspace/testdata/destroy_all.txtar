# setup repo
mkdir repo
cd repo
exec jj git init

exec $INCR workspace acquire --purpose destroy-one
envset WORKSPACE_ONE stdout

exec $INCR workspace acquire --purpose destroy-two
envset WORKSPACE_TWO stdout

exists $WORKSPACE_ONE
exists $WORKSPACE_TWO

exec $INCR workspace list
stdout 'ws-001'
stdout 'ws-002'

exec $INCR workspace destroy-all

! exists $WORKSPACE_ONE
! exists $WORKSPACE_TWO

exec $INCR workspace list
stdout 'No workspaces found'

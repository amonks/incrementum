# setup repo
mkdir repo
cd repo
exec jj git init

exec $INCR workspace acquire --purpose list-json

exec $INCR workspace list --json
stdout '"Name": "ws-001"'
stdout '"Status": "acquired"'
stdout '"Purpose": "list-json"'
